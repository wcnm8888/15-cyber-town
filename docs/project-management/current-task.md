# 当前任务

状态：`no_active_task`。

当前无活动任务。最近关闭任务 `F-001 工程与契约基线` 的任务卡与实现计划位于 [`../archive/task-cards/`](../archive/task-cards/)；该归档随 PR #1 合并进入 `main`，合并事实以 GitHub 为准。

下一任务必须由用户从 [`roadmap.md`](roadmap.md) 选择并批准任务卡后才能开始。不得自动进入 R-02。
